# MCP server SDK (with CLI helpers like `mcp dev` for the Inspector).
# Pinned to the 1.x line, which ships the FastMCP high-level API used here.
# (mcp 2.x renamed/restructured this into MCPServer.)
mcp[cli]>=1.4.0,<2.0.0

# Real DNS lookups (used later for MX-record verification)
dnspython
